{
  "name": "realestate-dapp-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Real Estate DApp",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.5",
    "ethers": "^6.11.0",
    "hardhat": "^2.19.0"
  }
}
